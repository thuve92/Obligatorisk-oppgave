// funksjon for home knapp.
function homeButton() {
    window.location.href="index.html";
}
  
// funksjon for å legge til data i modellen. 
// Den må kunne legge til informasjon til addSeries.
// Så må jeg pushe informasjonen i addSeries inn i modellen.

let inputTitle = '';

function getData(inputText) {
    inputTitle = inputText;
}
// funksjon save series.

function saveSeries() {
    let x =  {
        title: inputTitle,
        score: 0,
        review: '',
        image: "Warrior.jpg",
    };
    model.series.push(x);

    inputText = "";
    mainPageView();
}


// funksjon for å vise frem data fra modellen på library siden.
// For(loop) som skal lage diver som skal styles som serieforsider.

function showSeries() {
    let html = "";
    for(let i = 0; i < model.series.length; i++) {
        html += `
                <div onclick="seriesView(${i})" style=" padding: 20px; margin: 5px; height: 108px; width: 192px; border: solid black 2px;"><img src="${model.series[i].image}" style="height: 100%; width: 100%;">
                    <div style="text-align: center;">${model.series[i].title}</div>
                    
                </div>
                
        
        `;
    
    }
    return html; 
}

// funksjon for å kunne slette en hel serie.

function deleteSeries(index) {
    model.series.splice(index, 1);
    libraryView();
}

//funksjon for å kunne endre innhold i model.

 function changeSeriesModel(index) {
     model.series[index].title = model.newSeries.title;
     model.series[index].score = model.newSeries.score;
     model.series[index].review = model.newSeries.review;
     model.series[index].image = model.newSeries.image;
    seriesView(index);
 }

function changeSeriesHelper(index) {
    model.newSeries = model.series[index];
}



// funksjon for å kunne søke opp en serie. Den vil mest sansynelig ikke
// bli laget med engang.

function searchForSeries() {

}

// function movieTrailerLoop() {
//     for (let i = 0; i = model.movieTrailers[i].length; i++) {
//         model.movieTrailers[i] = model.movieTrailers[i].length;
//         mainPageView(i);
//     }
// }



































































































































































































































































































































































































































































































































































































































































































































































console.log('ERIK')
console.log('ER')
console.log('BEST')
console.log('<3')





















// funksjon for å kunne søke opp en serie. Den vil mest sansynelig ikke
